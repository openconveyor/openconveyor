---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: clustertriggerclasses.openconveyor.ai
spec:
  group: openconveyor.ai
  names:
    kind: ClusterTriggerClass
    listKind: ClusterTriggerClassList
    plural: clustertriggerclasses
    singular: clustertriggerclass
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterTriggerClass is the Schema for the clustertriggerclasses
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClusterTriggerClass
            properties:
              filters:
                description: |-
                  filters are ANDed; the webhook is dropped if any fails.
                  Typical use: filter GitHub "issues" events to "labeled" action with
                  a specific label, or Linear updates to a specific stateId.
                items:
                  description: |-
                    WebhookFilter is a simple gjson equality filter. The webhook adapter
                    only emits a Task when every filter matches.
                  properties:
                    equals:
                      description: equals is the expected string value.
                      type: string
                    path:
                      description: path is a gjson path into the webhook body.
                      type: string
                  required:
                  - equals
                  - path
                  type: object
                type: array
              path:
                description: path is the HTTP path the adapter listens on, e.g. "/linear"
                  or "/github".
                type: string
              signature:
                description: signature declares how to verify the inbound webhook.
                properties:
                  algorithm:
                    default: sha256
                    description: algorithm is the HMAC algorithm. Only "sha256" is
                      supported for now.
                    type: string
                  header:
                    description: |-
                      header is the HTTP header that carries the signature,
                      e.g. "X-Hub-Signature-256" (GitHub), "Linear-Signature".
                    type: string
                  prefix:
                    description: |-
                      prefix is stripped from the header value before comparison, e.g. "sha256="
                      for GitHub's "sha256=<hex>" format.
                    type: string
                  secretRef:
                    description: |-
                      secretRef names the Secret containing the shared signing secret.
                      The Secret must live in the same namespace as the webhook adapter.
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - header
                - secretRef
                type: object
              task:
                description: task is the Task template the adapter materializes.
                properties:
                  agent:
                    description: agent is the AgentRef copied into every emitted Task.
                    properties:
                      config:
                        description: config is passed to the agent image as-is (mounted
                          as JSON at /task/config.json).
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      ref:
                        description: ref is the name of a ClusterAgentClass.
                        type: string
                    required:
                    - ref
                    type: object
                  generateNamePrefix:
                    default: task-
                    description: |-
                      generateNamePrefix is prepended to a hash of the event payload to form
                      the Task name, unless a "name" FieldMapping overrides it.
                    type: string
                  mappings:
                    description: mappings populate fields of the emitted Task from
                      the webhook payload.
                    items:
                      description: |-
                        FieldMapping extracts a value from the webhook payload (JSON) via
                        a gjson-style path and maps it to a Task spec field.
                      properties:
                        default:
                          description: default is used if the path does not resolve.
                          type: string
                        from:
                          description: |-
                            from is a gjson path into the webhook body, e.g.
                            "issue.title" or "pull_request.head.ref".
                          type: string
                        to:
                          description: |-
                            to is the target Task field. Accepted values:
                              "prompt"              — body of Task.spec.prompt.inline
                              "labels.<key>"        — set a label on the Task
                              "annotations.<key>"   — set an annotation on the Task
                              "name"                — override the generated Task name
                          type: string
                      required:
                      - from
                      - to
                      type: object
                    type: array
                  namespace:
                    description: namespace is where Tasks will be created. Defaults
                      to the adapter's namespace.
                    type: string
                  permissions:
                    description: permissions is copied into every emitted Task.
                    properties:
                      egress:
                        description: |-
                          egress is the allowlist of DNS names or CIDR blocks the Task may reach.
                          Default-deny otherwise. Resolved to NetworkPolicy egress rules at reconcile time.
                        items:
                          type: string
                        type: array
                      rbac:
                        description: |-
                          rbac is the list of Kubernetes RBAC rules the Task needs. Empty means
                          the Task has no ServiceAccount token and cannot talk to the k8s API.
                        items:
                          description: |-
                            RBACRule is a minimal Kubernetes RBAC policy rule. Mirrors rbacv1.PolicyRule
                            but kept local so the Task CRD schema does not transitively embed rbac/v1.
                          properties:
                            apiGroups:
                              items:
                                type: string
                              type: array
                            resourceNames:
                              items:
                                type: string
                              type: array
                            resources:
                              items:
                                type: string
                              type: array
                            verbs:
                              items:
                                type: string
                              type: array
                          required:
                          - apiGroups
                          - resources
                          - verbs
                          type: object
                        type: array
                      secrets:
                        description: |-
                          secrets is the list of Secret names (in the Task's namespace) projected
                          as files at /run/secrets/<name>. Env-var injection is intentionally not
                          supported — env vars leak into /proc.
                        items:
                          type: string
                        type: array
                    type: object
                  resources:
                    description: resources is copied into every emitted Task.
                    properties:
                      cpu:
                        description: cpu request/limit, e.g. "500m" or "1".
                        type: string
                      memory:
                        description: memory request/limit, e.g. "512Mi" or "1Gi".
                        type: string
                      timeout:
                        description: |-
                          timeout is mandatory. The controller rejects Tasks without one.
                          Mapped to Job.spec.activeDeadlineSeconds.
                        type: string
                    required:
                    - timeout
                    type: object
                required:
                - agent
                - resources
                type: object
            required:
            - path
            - signature
            - task
            type: object
          status:
            description: status defines the observed state of ClusterTriggerClass
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

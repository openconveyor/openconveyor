---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: clusteragentclasses.openconveyor.ai
spec:
  group: openconveyor.ai
  names:
    kind: ClusterAgentClass
    listKind: ClusterAgentClassList
    plural: clusteragentclasses
    singular: clusteragentclass
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterAgentClass is the Schema for the clusteragentclasses API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ClusterAgentClass
            properties:
              args:
                description: args overrides the image CMD.
                items:
                  type: string
                type: array
              command:
                description: command overrides the image ENTRYPOINT.
                items:
                  type: string
                type: array
              image:
                description: image is the OCI reference for the agent.
                type: string
              imagePullPolicy:
                default: IfNotPresent
                description: imagePullPolicy mirrors Pod.spec.containers[].imagePullPolicy.
                type: string
              inputs:
                description: inputs describes how Task data reaches the agent container.
                properties:
                  config:
                    description: config is where Task.spec.agent.config is delivered
                      (JSON).
                    properties:
                      env:
                        description: |-
                          env is the environment variable name. Mutually exclusive with mount.
                          Prefer mount for secrets — env vars leak into /proc.
                        type: string
                      mount:
                        description: |-
                          mount is an absolute filesystem path inside the container. Mutually
                          exclusive with env.
                        type: string
                    type: object
                  prompt:
                    description: prompt is where Task.spec.prompt is delivered.
                    properties:
                      env:
                        description: |-
                          env is the environment variable name. Mutually exclusive with mount.
                          Prefer mount for secrets — env vars leak into /proc.
                        type: string
                      mount:
                        description: |-
                          mount is an absolute filesystem path inside the container. Mutually
                          exclusive with env.
                        type: string
                    type: object
                type: object
              requires:
                description: |-
                  requires declares permissions this agent always needs. Additive with
                  Task.spec.permissions at reconcile time.
                properties:
                  egress:
                    description: egress allowlist the agent always needs (e.g. the
                      LLM endpoint).
                    items:
                      type: string
                    type: array
                  secrets:
                    description: secrets the agent always needs mounted.
                    items:
                      type: string
                    type: array
                type: object
            required:
            - image
            type: object
          status:
            description: status defines the observed state of ClusterAgentClass
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
